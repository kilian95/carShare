%h4 Most recent lift offers:


-@offers.each do |offer|
	.container
		.box.panel.panel-default 
			%h3= link_to offer.departure, offer
			to
			%h3= link_to offer.destination, offer
			=offer.time.strftime("%dth %B,  %H:%M")
			%br/
			Number of seats available:
			%strong
				=offer.seats
			%br/
			Posted by:
			=link_to "#{offer.user.fname} #{offer.user.lname}", user_path(offer.user_id)
			%br/
			.avatar
				=image_tag offer.user.avatar.url.to_s
			

